{
  "name": "@repo/e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end suite: drives the real UI against the real API in a real browser",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
